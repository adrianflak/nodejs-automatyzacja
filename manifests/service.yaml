apiVersion: v1
kind: Service
metadata:
  name: prodaks
  namespace: dev
  labels:
    app.kubernetes.io/name: prodaks
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: prodaks
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000